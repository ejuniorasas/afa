<p id="notice"><%= notice %></p>

<h1>Families</h1>


<div class="pricing-header px-3 py-3 pt-md-5 pb-md-4 mx-auto text-center">
  <h1 class="display-4">AFA</h1>
  <p class="lead">Associacó de Familiares I Alumins des Conservatori I Escola Estadual ...
    <a href="<%=new_family_path %>" class="btn btn-primary rounded-pill"> Adicionar Familia</a>
  </p>
  
</div>
<div class="container">
  <div class="card-deck mb-2 text-center">  

   <% @families.each do |family| %>
    <div class="mb-2">
        <a class="btn btn-sm" href="<%=edit_family_path(family)%>" @click='showFamily(family)'>
          <svg class="bd-placeholder-img rounded-circle active" width="150" height="150" 
          xmlns="http://www.w3.org/2000/svg" preserveAspectRatio="xMidYMid slice" 
          focusable="false" role="img">  <title><%= family.name %></title>
          <rect width="100%" height="100%" class='_<%= family.status %>'/><text y=50% x=10% class='_<%= family.status %>' dy=".3em"><%= family.name %></text></svg>
        </a>
      </div>
      <% end %>
    </div>

  </div>

<br>

<%= link_to 'New Family', new_family_path %>
